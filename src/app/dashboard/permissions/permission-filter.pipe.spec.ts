import { PermissionFilterPipe } from './permission-filter.pipe';

describe('PermissionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PermissionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
