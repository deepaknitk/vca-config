import { DocumentClassificationFilterPipe } from './document-classification-filter.pipe';

describe('DocumentClassificationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentClassificationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
